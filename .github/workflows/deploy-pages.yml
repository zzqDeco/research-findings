name: Deploy project site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout application source
        uses: actions/checkout@v7

      - name: Checkout generated report data
        id: reports
        continue-on-error: true
        uses: actions/checkout@v7
        with:
          ref: reports-data
          path: .reports-data

      - name: Restore generated reports
        shell: bash
        run: |
          mkdir -p public/reports
          if [[ -d .reports-data/ai-hotspot-daily ]]; then
            cp -R .reports-data/ai-hotspot-daily public/reports/
          fi
          if [[ -d .reports-data/polymarket-daily ]]; then
            cp -R .reports-data/polymarket-daily public/reports/
          fi
          if [[ -d .reports-data/juya-rss-daily ]]; then
            cp -R .reports-data/juya-rss-daily public/reports/
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build project site
        env:
          VITE_BASE_PATH: /${{ github.event.repository.name }}/
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
